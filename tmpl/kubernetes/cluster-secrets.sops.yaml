---
apiVersion: v1
kind: Secret
metadata:
  name: cluster-secrets
  namespace: flux-system
stringData:
  SECRET_DOMAIN: "${BOOTSTRAP_CLOUDFLARE_DOMAIN}"
  SECRET_CLOUDFLARE_EMAIL: "${BOOTSTRAP_CLOUDFLARE_EMAIL}"
  SECRET_EXT_DOMAIN: "${BOOTSTRAP_CLOUDFLARE_EXT_DOMAIN}"
  SECRET_CLOUDFLARED_TUNNEL_ID: "${BOOTSTRAP_CLOUDFLARED_TUNNEL_ID}"
